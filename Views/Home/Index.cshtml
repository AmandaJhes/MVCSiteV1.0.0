@model HomeModel;
@{
    ViewData["Title"] = "Home Page";
}

<div class="text-center">
    <h1 class="display-4">Seja bem vinda, @Model.Nome !</h1>
    <p>Meu primeiro site MVC.</p>
    <span>Seu email é @Model.Email</span>
</div>
